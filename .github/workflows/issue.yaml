name: Issue Echo

on:
  issues:
    types: [opened]

jobs:
  echo-title:
    runs-on: ubuntu-latest
    steps:
      - name: Display Issue Title
        run: echo "Issue title is ${{ github.event.issue.title }}"
